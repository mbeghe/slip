{
  "name": "slip",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @slip/api\" \"npm run dev -w @slip/web\"",
    "build": "npm run build -w @slip/web && npm run build -w @slip/api",
    "start": "npm run start -w @slip/api",
    "db:seed": "npm run db:seed -w @slip/api",
    "db:sync-prices": "npm run db:sync-prices -w @slip/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
